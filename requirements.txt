# Pinned 2026-06-05 — a 2026-05-20 incidens után. A fastmcp pin KRITIKUS:
# pin nélkül minden Railway-rebuild a legfrissebb major verziót húzza
# (időzített bomba breaking change-eknél). Railway-n verifikált: 3.4.0.
fastmcp==3.4.0
httpx>=0.28,<1.0
starlette>=0.27.0
google-api-python-client>=2.100.0
google-auth>=2.20.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.2.0
matplotlib>=3.6,<4.0
# DELPHOI persona_sampler (scipy.stats.entropy) + ssr (numpy). numpy eddig csak
# a matplotlib tranzitív függőségeként jött — expliciten pinelve, hogy ne törjön.
numpy>=1.24,<3.0
scipy>=1.11,<2.0
python-docx>=1.0.0
PyPDF2>=3.0.0
# OPERATION LESESAAL (2026-08-07) — Réka dokumentum-pipeline-ja.
# PyMuPDF: szövegréteg + SZKENNELT oldal raszterizálása + beágyazott ÁBRÁK
# kiemelése. A PyPDF2 egyiket sem tudja, és egy szakcikkben az információ
# fele a Figure-ökben van, nem a bekezdésekben.
PyMuPDF>=1.24,<2.0
# Pillow + pillow-heif: telefonos fotó normalizálása (HEIC, MPO-multiframe,
# EXIF-forgatás) és TELJES EXIF-strip — a GPS ne menjen fel külső API-ra.
# Ugyanez a normalizáló a task #20 (Agora fotófeltöltés) közös pipeline-ja.
Pillow>=10.0,<12.0
pillow-heif>=0.16
openpyxl>=3.1.0
python-pptx>=0.6.23
# PYTHIA B2 (riport-mű): mindkettő NOTSTROM-fallbackkal fut a kódban —
# jinja2 nélkül f-string sablon, weasyprint nélkül HTML a PDF-fallback.
jinja2>=3.1,<4.0
weasyprint>=60
# PYTHIA P4 (pénz-út): Stripe-Python SDK — NOTSTROM-fallbackkal fut a kódban
# (plugins/delphoi_stripe.py: SDK nélkül sima REST-hívás httpx-szel).
stripe>=15,<16

# ── Cikk-kivonatolás (2026-08-30) ─────────────────────────────────────────
# MIÉRT KERÜLT BE: a `_fetch_page_contents` tiszta regexszel strippelt, majd
# 2000 karakterre vágott. Mérve 10 valódi hírcikken: a kimenet 68%-a
# boilerplate (medián 7052 -> 2169 karakter), és 4/10 cikknél a LEAD ki sem
# fért a 2000 karakteres ablakba — a guardian és az ansa cikkéből NULLA
# bekezdés jutott az LLM-hez, csak navigáció. A regex a HTML-entitásokat sem
# dekódolta (`isn&#x27;t`), ezért a bbc leadje egyáltalán nem volt megtalálható.
# Ára: +20 ms/oldal (1,6 ms -> 21 ms medián). Cserébe ~8x kevesebb token
# ugyanazért a cikkért.
#
# ⚠️ Mindkettő NOTSTROM-fallbackkal fut a kódban (trafilatura -> readability
# -> regex), tehát a hiányuk nem boot-kritikus — DE a hiányuk NÉMA
# minőségromlás, és pontosan ez történt: 2026-08-30-ig egyik sem volt itt,
# miközben a fejlesztői gépen a ~/.local-ból feloldódtak. A helyi teszt jónak
# látszott, a prod regexet futtatott.
trafilatura>=1.12
readability-lxml>=0.8
