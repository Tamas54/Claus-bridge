# Pinned 2026-06-05 — a 2026-05-20 incidens után. A fastmcp pin KRITIKUS:
# pin nélkül minden Railway-rebuild a legfrissebb major verziót húzza
# (időzített bomba breaking change-eknél). Railway-n verifikált: 3.4.0.
fastmcp==3.4.0
httpx>=0.28,<1.0
starlette>=0.27.0
google-api-python-client>=2.100.0
google-auth>=2.20.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.2.0
matplotlib>=3.6,<4.0
# DELPHOI persona_sampler (scipy.stats.entropy) + ssr (numpy). numpy eddig csak
# a matplotlib tranzitív függőségeként jött — expliciten pinelve, hogy ne törjön.
numpy>=1.24,<3.0
scipy>=1.11,<2.0
python-docx>=1.0.0
PyPDF2>=3.0.0
openpyxl>=3.1.0
python-pptx>=0.6.23
# PYTHIA B2 (riport-mű): mindkettő NOTSTROM-fallbackkal fut a kódban —
# jinja2 nélkül f-string sablon, weasyprint nélkül HTML a PDF-fallback.
jinja2>=3.1,<4.0
weasyprint>=60
# PYTHIA P4 (pénz-út): Stripe-Python SDK — NOTSTROM-fallbackkal fut a kódban
# (plugins/delphoi_stripe.py: SDK nélkül sima REST-hívás httpx-szel).
stripe>=15,<16

